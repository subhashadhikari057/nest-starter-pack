# Fetch native binaries for both the local dev platform and linux-x64 (server)
# so pnpm deploy bundles include the correct sharp binary for deployment.
supported-architectures-os[]=current
supported-architectures-os[]=linux
supported-architectures-cpu[]=current
supported-architectures-cpu[]=x64
